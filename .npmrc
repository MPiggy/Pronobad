# Prisma 7 requires Node >= 22.12; on Node 20 it fails deep inside a bundled
# CommonJS file with an opaque ERR_REQUIRE_ESM rather than a version error.
# `engines` in package.json is only a warning by default — this makes the
# mismatch fail immediately, with the actual reason.
engine-strict=true
